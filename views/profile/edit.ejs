<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300&display=swap" rel="stylesheet">
    <link rel='stylesheet' href='/stylesheets/styleedit.css' />
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Secrets make friends</title>
</head>

<body>
    <div id="edit-container">
    <nav>
        <% if (user) { %>

            <a href="/logout">LOG OUT</a>
            <% } else { %>
                <a href="/auth/google" class="login">LOG IN&nbsp;<img src="https://i.imgur.com/FHjYyi0.png"></a>
                <% } %>
                    <a href="/../secrets/new">HOME</a>
                    <a href="/../secrets">SECRETS</a>
                    <a href="/profile/<%= user._id %>">YOUR PROFILE</a>


    </nav>
   <div id="edit-section">
    <div id="edit-pic">
    <div id="pic-form">
    <form action="/profile/<%= user._id %>?_method=PUT" method="POST" autocomplete="off">
        <input 
        id="pic-input"
        type="text" 
        name="img" 
        placeholder="image or gif URL">
        <input 
        type="submit" 
        id="button-pic" 
        value="save">
    </form>
</div>
    <img id="profile-pic" src="<%= user.img %>">
    </div>
    <div id="edit-username">
        <h1 id="user-name">Username: <%= user.name %></h1>
        <div id="username-form">
        <form action="/profile/<%= user._id %>?_method=PUT" method="POST" autocomplete="off" id="userform">
            <input 
            id="username-input"
            type="text" 
            name="name" 
            placeholder="New Username">
            <input 
            type="submit" 
            id="button-username" 
            value="save">
        </form>
    </div>
       
     
    </div>
</div>
</div>


</body>

</html>